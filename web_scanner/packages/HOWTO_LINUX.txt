╔══════════════════════════════════════════════════════════════════════╗
║                                                                      ║
║         ULTIMATE SECURITY TESTER - LINUX HOW-TO GUIDE               ║
║                    Step-by-Step Instructions                         ║
║                                                                      ║
╚══════════════════════════════════════════════════════════════════════╝

📋 TABLE OF CONTENTS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
1. System Requirements
2. Installation Steps
3. Running Your First Scan
4. Advanced Usage
5. Reading Reports
6. Troubleshooting
7. Tips & Best Practices

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

╔══════════════════════════════════════════════════════════════════════╗
║  SECTION 1: SYSTEM REQUIREMENTS                                      ║
╚══════════════════════════════════════════════════════════════════════╝

Supported Distributions:
✓ Ubuntu 20.04 / 22.04 / 24.04
✓ Debian 11 / 12
✓ Kali Linux 2023+
✓ Fedora 38+
✓ CentOS / RHEL 8+
✓ Arch Linux

Minimum Requirements:
✓ 1 GB RAM
✓ 100 MB free disk space
✓ Internet connection
✓ sudo / root privileges

Recommended:
✓ 2 GB RAM or more
✓ 500 MB free disk space
✓ Stable internet connection

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

╔══════════════════════════════════════════════════════════════════════╗
║  SECTION 2: INSTALLATION STEPS                                       ║
╚══════════════════════════════════════════════════════════════════════╝

UBUNTU / DEBIAN / KALI LINUX
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

STEP 1: Update System
sudo apt update && sudo apt upgrade -y

STEP 2: Install Python and pip
sudo apt install python3 python3-pip -y

STEP 3: Verify Python Installation
python3 --version
# Should show: Python 3.x.x

STEP 4: Install Required Packages
# Method A: Using pip (Recommended)
pip3 install requests beautifulsoup4 lxml urllib3

# Method B: Using apt (Alternative)
sudo apt install python3-requests python3-bs4 python3-lxml python3-urllib3 -y

STEP 5: Verify Installation
pip3 list | grep -E "requests|beautifulsoup4|lxml|urllib3"

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

FEDORA / RHEL / CENTOS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

STEP 1: Update System
sudo dnf update -y

STEP 2: Install Python and pip
sudo dnf install python3 python3-pip -y

STEP 3: Install Required Packages
pip3 install requests beautifulsoup4 lxml urllib3

STEP 4: Verify Installation
python3 --version
pip3 list | grep -E "requests|beautifulsoup4|lxml|urllib3"

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

ARCH LINUX
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

STEP 1: Update System
sudo pacman -Syu

STEP 2: Install Python and pip
sudo pacman -S python python-pip

STEP 3: Install Required Packages
pip3 install requests beautifulsoup4 lxml urllib3

STEP 4: Verify Installation
python3 --version
pip3 list | grep -E "requests|beautifulsoup4|lxml|urllib3"

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

AUTOMATED INSTALLATION SCRIPT
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Save this as setup_linux.sh and run: bash setup_linux.sh

#!/bin/bash
echo "==================================="
echo "Ultimate Security Tester Setup"
echo "==================================="

# Detect distribution
if [ -f /etc/debian_version ]; then
    echo "[*] Detected Debian/Ubuntu"
    sudo apt update
    sudo apt install python3 python3-pip -y
elif [ -f /etc/fedora-release ]; then
    echo "[*] Detected Fedora"
    sudo dnf install python3 python3-pip -y
elif [ -f /etc/arch-release ]; then
    echo "[*] Detected Arch Linux"
    sudo pacman -S python python-pip
else
    echo "[!] Unknown distribution"
    echo "[*] Trying generic installation..."
fi

echo "[*] Installing Python packages..."
pip3 install requests beautifulsoup4 lxml urllib3

echo "[+] Installation complete!"
echo "[*] Test with: python3 ultimate_tester.py -u http://testphp.vulnweb.com"

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

╔══════════════════════════════════════════════════════════════════════╗
║  SECTION 3: RUNNING YOUR FIRST SCAN                                  ║
╚══════════════════════════════════════════════════════════════════════╝

SCAN 1: Basic Test Scan
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Test against a safe website (testphp.vulnweb.com)

1. Open terminal (Ctrl + Alt + T)

2. Navigate to the script directory
   cd /path/to/your/folder
   # Example: cd ~/Downloads/scanner

3. Make script executable (first time only)
   chmod +x ultimate_tester.py

4. Run the scan
   python3 ultimate_tester.py -u http://testphp.vulnweb.com

5. Wait for scan to complete (10-30 seconds)

6. View the report in terminal and check JSON file

SCAN 2: Scan Your Own Website
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
⚠️ WARNING: Only scan websites you own or have permission to test!

python3 ultimate_tester.py -u http://your-website.com

SCAN 3: OWASP 2025 Scan
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Test against the latest OWASP Top 10:2025 standard

python3 ultimate_tester.py -u http://testphp.vulnweb.com --owasp-2025

SCAN 4: Using sudo (if needed)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Some operations may require root privileges

sudo python3 ultimate_tester.py -u http://testphp.vulnweb.com

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

╔══════════════════════════════════════════════════════════════════════╗
║  SECTION 4: ADVANCED USAGE                                           ║
╚══════════════════════════════════════════════════════════════════════╝

Command Options:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

-u, --url          Target URL (required)
-v, --verbose      Show detailed output
--burp-pro         Enable Burp Pro features
--owasp-2025       Use OWASP Top 10:2025 standard

Example Commands:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# Basic Scan (OWASP 2021)
python3 ultimate_tester.py -u http://testphp.vulnweb.com

# OWASP 2025 Scan
python3 ultimate_tester.py -u http://testphp.vulnweb.com --owasp-2025

# Burp Pro Features
python3 ultimate_tester.py -u http://testphp.vulnweb.com --burp-pro

# Full Scan (OWASP 2025 + Burp Pro)
python3 ultimate_tester.py -u http://testphp.vulnweb.com --owasp-2025 --burp-pro

# Verbose Output
python3 ultimate_tester.py -u http://testphp.vulnweb.com -v

# HTTPS Website
python3 ultimate_tester.py -u https://example.com

# Local Network Target
python3 ultimate_tester.py -u http://192.168.1.100

# With Port Number
python3 ultimate_tester.py -u http://example.com:8080

# Save output to file
python3 ultimate_tester.py -u http://testphp.vulnweb.com | tee scan_output.txt

# Run in background
nohup python3 ultimate_tester.py -u http://testphp.vulnweb.com &

# Multiple scans in sequence
python3 ultimate_tester.py -u http://site1.com && \
python3 ultimate_tester.py -u http://site2.com

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

╔══════════════════════════════════════════════════════════════════════╗
║  SECTION 5: READING REPORTS                                          ║
╚══════════════════════════════════════════════════════════════════════╝

Terminal Output Colors:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🔴 RED     = Critical/High severity findings
🟡 YELLOW  = Medium severity findings  
🔵 CYAN    = Low severity findings
🟢 GREEN   = Passed checks

JSON Report Location:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Reports are saved in current directory as: scan_TIMESTAMP.json

View JSON Report:
# Pretty print JSON
cat scan_*.json | python3 -m json.tool

# View with less
less scan_*.json

# Open with text editor
nano scan_*.json
vim scan_*.json

# Search for specific severity
grep -i "critical" scan_*.json
grep -i "high" scan_*.json

Severity Levels:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
CRITICAL - Fix immediately! System compromise possible
HIGH     - Fix urgently! Unauthorized access possible
MEDIUM   - Fix soon! Security misconfiguration
LOW      - Fix when possible! Minor security issue

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

╔══════════════════════════════════════════════════════════════════════╗
║  SECTION 6: TROUBLESHOOTING                                          ║
╚══════════════════════════════════════════════════════════════════════╝

Problem: "python3: command not found"
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Solution: Install Python

# Ubuntu/Debian
sudo apt install python3 python3-pip

# Fedora
sudo dnf install python3 python3-pip

# Arch
sudo pacman -S python python-pip

Problem: "No module named 'requests'"
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Solution: Install required packages

pip3 install requests beautifulsoup4 lxml urllib3

# Or with sudo if needed
sudo pip3 install requests beautifulsoup4 lxml urllib3

Problem: "Permission denied"
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Solution 1: Make script executable
chmod +x ultimate_tester.py

Solution 2: Run with sudo
sudo python3 ultimate_tester.py -u http://testphp.vulnweb.com

Problem: SSL Certificate Error
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Solution: Update certificates

sudo apt install ca-certificates
pip3 install --upgrade certifi

Problem: Slow or hanging scan
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Solution: Press Ctrl+C to stop, then:
- Check target URL is correct
- Verify internet connection
- Try scanning test website first
- Use verbose mode (-v) to see progress

Problem: "ImportError: cannot import name"
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Solution: Reinstall packages

pip3 uninstall requests beautifulsoup4 lxml urllib3
pip3 install requests beautifulsoup4 lxml urllib3

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

╔══════════════════════════════════════════════════════════════════════╗
║  SECTION 7: TIPS & BEST PRACTICES                                    ║
╚══════════════════════════════════════════════════════════════════════╝

Legal & Ethical:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
⚠️ Only scan:
   ✓ Your own websites
   ✓ Test environments  
   ✓ Sites with written permission

⚠️ Never scan:
   ✗ Websites you don't own
   ✗ Production systems without permission
   ✗ Government or military sites
   ✗ Banking or financial systems

Command Line Tips:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# Create alias for quick access
echo "alias ultimate='python3 /path/to/ultimate_tester.py'" >> ~/.bashrc
source ~/.bashrc

# Now you can use:
ultimate -u http://testphp.vulnweb.com

# Run multiple scans
for url in site1.com site2.com site3.com; do
    python3 ultimate_tester.py -u http://$url
done

# Schedule scans with cron
crontab -e
# Add: 0 2 * * * python3 /path/to/ultimate_tester.py -u http://yoursite.com

Performance Tips:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
✓ Standard scan: 10-30 seconds
✓ Full scan (--burp-pro): 30-60 seconds
✓ Use wired connection for best results
✓ Close unnecessary applications
✓ Run during off-peak hours

Report Management:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# Organize reports by date
mkdir -p ~/security_reports/$(date +%Y-%m-%d)
mv scan_*.json ~/security_reports/$(date +%Y-%m-%d)/

# Compare scans
diff scan_old.json scan_new.json

# Archive old reports
tar -czf reports_$(date +%Y%m%d).tar.gz scan_*.json

Security Best Practices:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
✓ Keep scanner updated
✓ Protect scan reports (contain sensitive info)
✓ Use VPN when scanning remote targets
✓ Log all scanning activities
✓ Get written permission before scanning

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

╔══════════════════════════════════════════════════════════════════════╗
║  QUICK REFERENCE CARD                                                ║
╚══════════════════════════════════════════════════════════════════════╝

Installation (Ubuntu/Debian):
┌──────────────────────────────────────────────────────────────────┐
│ sudo apt update                                                  │
│ sudo apt install python3 python3-pip -y                         │
│ pip3 install requests beautifulsoup4 lxml urllib3               │
└──────────────────────────────────────────────────────────────────┘

Common Commands:
┌──────────────────────────────────────────────────────────────────┐
│ Basic Scan                                                       │
│ python3 ultimate_tester.py -u http://testphp.vulnweb.com        │
├──────────────────────────────────────────────────────────────────┤
│ OWASP 2025 Scan                                                  │
│ python3 ultimate_tester.py -u http://testphp.vulnweb.com --owasp-2025 │
├──────────────────────────────────────────────────────────────────┤
│ Full Scan                                                        │
│ python3 ultimate_tester.py -u http://testphp.vulnweb.com --owasp-2025 --burp-pro │
└──────────────────────────────────────────────────────────────────┘

Troubleshooting:
┌──────────────────────────────────────────────────────────────────┐
│ Make executable: chmod +x ultimate_tester.py                    │
│ Check Python: python3 --version                                 │
│ Check packages: pip3 list | grep requests                       │
│ View report: cat scan_*.json | python3 -m json.tool            │
└──────────────────────────────────────────────────────────────────┘

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

╔══════════════════════════════════════════════════════════════════════╗
║                         END OF GUIDE                                 ║
║                     Good luck with your scanning!                    ║
╚══════════════════════════════════════════════════════════════════════╝
